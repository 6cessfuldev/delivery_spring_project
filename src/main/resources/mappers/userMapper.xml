<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ezen.delivery.repository.UserDAO">
	<select id="getUser" resultType="com.ezen.delivery.domain.UserVO">
		select * from user 
		where user_email = #{user_email}
	</select>
	
	<insert id="insertUser">
		insert into user (user_email, user_pw, user_name, user_phone, user_birth, user_nick)
		values (#{user_email}, #{user_pw}, #{user_name}, #{user_phone}, #{user_birth}, #{user_nick})
	</insert>
</mapper>